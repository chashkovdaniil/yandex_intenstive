# При загрузке новых плагинов стоит полностью перезапускать приложение

name: useapi
description: Test project for use api

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

# Здесь устанавливать новые пакеты; после записи нового пакета, необходимо
# нажать на кнопу 'Pub get'
dependencies:
  http: ^0.13.4
  dio:  ^4.0.4
